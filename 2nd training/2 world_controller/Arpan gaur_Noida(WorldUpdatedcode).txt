public class World {
	Map<String, List<String>> countryToStateMap;
    Map<String, String> countryToCapital;
    List<String> stateList;
    public World(){
        countryToStateMap = new Map<String, List<String>>();
        countryToCapital = new Map<String, String>();
        For(World__c world : [SELECT Id, Name, Capital__c, State__c 
                              FROM World__C ]){
            if(countryToStateMap.containsKey(World.Name)){
                List<String> stateList = countryToStateMap.get(World.Name);
                stateList.add(World.State__c);
                countryToStateMap.put(World.Name, stateList);
                //system.debug(stateList);
            }
            else{
                List<String> stateList = new List<String>();
                stateList.add(world.State__c);
                countryToStateMap.put(world.Name, stateList);
            }
            if(world.Capital__c){
                countryToCapital.put(world.Name,world.State__c);
            }
        }
    }
    
    Public List<String> getStates(String countryName){
    	return countryToStateMap.get(countryName);
    }
    
    Public String getCapital(String countryName){
    	return countryToCapital.get(countryName);
    }
    
}
