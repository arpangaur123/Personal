<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CS_Contact_No_New__c</fullName>
    <externalId>false</externalId>
    <formula>IF( AND( Owner:User.Phone != NULL, Owner:User.MobilePhone == NULL),  Owner:User.Phone, 
  IF(AND( Owner:User.Phone == NULL, Owner:User.MobilePhone != NULL), Owner:User.MobilePhone,
    IF(AND( Owner:User.Phone != NULL, Owner:User.MobilePhone != NULL), Owner:User.Phone, null
    )
  )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>CS Contact No.</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
